{% extends 'frontend_layout/index.html.twig' %}

{% block title %}{{ 'Patient Profile'|trans }} - {{ patient.firstName }} {{ patient.lastName }}{% endblock %}

{% block body %}
<div class="container mt-5 pt-5 pb-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <!-- Patient Header -->
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-4">
                <div class="card-body p-5" style="background: linear-gradient(135deg, var(--brand-color), var(--accent-color)); color: white;">
                    <div class="row align-items-center">
                        <div class="col-md-auto text-center mb-3 mb-md-0">
                            <img src="{{ patient.avatar ? (patient.avatar|slice(0, 4) == 'http' or patient.avatar|slice(0, 1) == '/' ? patient.avatar : '/uploads/avatars/' ~ patient.avatar) : '/img/default-avatar.png' }}" 
                                 class="rounded-circle border border-4 border-white shadow" 
                                 style="width: 120px; height: 120px; object-fit: cover;" alt="Patient">
                        </div>
                        <div class="col text-center text-md-start">
                            <div class="badge bg-white bg-opacity-25 rounded-pill px-3 py-1 mb-2 small text-uppercase fw-bold">{{ 'Digital Health ID'|trans }}</div>
                            <h2 class="fw-bold mb-1 text-white">{{ patient.firstName }} {{ patient.lastName }}</h2>
                            <p class="mb-3 opacity-75"><i class="bi bi-envelope me-1"></i> {{ patient.email }}</p>
                            <div class="d-flex flex-wrap justify-content-center justify-content-md-start gap-3">
                                <div class="bg-white bg-opacity-10 rounded-pill px-3 py-1 small">
                                    <i class="bi bi-telephone me-1"></i> {{ patient.phone|default('Not provided'|trans) }}
                                </div>
                                <div class="bg-white bg-opacity-10 rounded-pill px-3 py-1 small">
                                    <i class="bi bi-calendar-check me-1"></i> {{ consultations|length }} {{ 'Total Consultations'|trans }}
                                </div>
                            </div>
                        </div>
                        <div class="col-md-auto mt-4 mt-md-0">
                            {% if doctor %}
                                <a href="{{ path('app_profile_chat', {'doctorId': doctor.id, 'patientId': patient.id}) }}" class="btn btn-white rounded-pill px-4 py-2 fw-bold shadow-sm">
                                    <i class="bi bi-chat-dots-fill me-2"></i> {{ 'Open Chat'|trans }}
                                </a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>

            <!-- Consultation History -->
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-4">
                <div class="card-header bg-white border-0 py-4 px-4">
                    <h5 class="fw-bold mb-0"><i class="bi bi-clock-history text-primary me-2"></i>{{ 'Consultation History'|trans }}</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="bg-light text-muted small text-uppercase tracking-wider">
                                <tr>
                                    <th class="ps-4 border-0">{{ 'Date'|trans }}</th>
                                    <th class="border-0">{{ 'Hospital'|trans }}</th>
                                    <th class="border-0">{{ 'Status'|trans }}</th>
                                    <th class="border-0 pe-4 text-end">{{ 'Action'|trans }}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for consultation in consultations %}
                                <tr>
                                    <td class="ps-4">
                                        <div class="fw-bold">{{ consultation.requestedDate|date('M d, Y') }}</div>
                                        <div class="small text-muted">{{ consultation.requestedDate|date('H:i') }}</div>
                                    </td>
                                    <td>
                                        <div class="small"><i class="bi bi-hospital me-1"></i> {{ consultation.hospital.name|default('N/A') }}</div>
                                    </td>
                                    <td>
                                        <span class="badge rounded-pill px-3 py-1 small 
                                            {% if consultation.status == 'confirmed' %}bg-success-subtle text-success
                                            {% elseif consultation.status == 'cancelled' %}bg-danger-subtle text-danger
                                            {% else %}bg-warning-subtle text-warning{% endif %}">
                                            {{ consultation.status|capitalize|trans }}
                                        </span>
                                    </td>
                                    <td class="pe-4 text-end">
                                        <a href="{{ path('app_consultation_process', {'id': consultation.id}) }}" class="btn btn-sm btn-outline-primary rounded-pill px-3">
                                            {{ 'View Records'|trans }}
                                        </a>
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="4" class="text-center py-5 text-muted italic">
                                        {{ 'No consultation history found for this patient.'|trans }}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <!-- Patient's Doctors -->
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-4">
                <div class="card-header bg-white border-0 py-4 px-4">
                    <h5 class="fw-bold mb-0"><i class="bi bi-people text-primary me-2"></i>{{ 'Կցված մասնագետներ'|trans }}</h5>
                </div>
                <div class="card-body p-4">
                    <div class="row g-3">
                        {% for dr in patient_doctors %}
                            <div class="col-12">
                                <div class="d-flex align-items-center p-3 bg-light rounded-4 border border-white shadow-sm transition-all">
                                    <img src="{{ dr.user.avatar ? (dr.user.avatar|slice(0, 4) == 'http' or dr.user.avatar|slice(0, 1) == '/' ? dr.user.avatar : '/uploads/avatars/' ~ dr.user.avatar) : '/img/default-avatar.png' }}" 
                                         class="rounded-circle me-4" 
                                         style="width: 60px; height: 60px; object-fit: cover;" alt="Doctor">
                                    <div class="flex-grow-1">
                                        <div class="fw-bold text-dark fs-5">{{ dr.user.firstName }} {{ dr.user.lastName }}</div>
                                        <div class="d-flex align-items-center gap-3">
                                            <span class="small text-muted"><i class="bi bi-envelope me-1"></i>{{ dr.user.email }}</span>
                                            <span class="badge bg-primary-subtle text-primary rounded-pill px-3">{{ dr.specialty|default('Specialist'|trans) }}</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% else %}
                            <div class="col-12 text-center py-4 text-muted small">
                                <i class="bi bi-info-circle me-1"></i> {{ 'No other specialists are currently linked to this patient.'|trans }}
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <!-- Action Area -->
            <div class="text-center mt-5">
                <a href="{{ path('app_doctor_dashboard') }}" class="btn btn-primary rounded-pill px-5 py-3 fw-bold shadow-lg text-white">
                    <i class="bi bi-arrow-left me-2"></i> {{ 'Back to Dashboard'|trans }}
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    .btn-white {
        background: white;
        color: var(--brand-color);
        border: none;
    }
    .btn-white:hover {
        background: #f8f9fa;
        transform: translateY(-2px);
    }
    .hover-translate:hover {
        transform: translateY(-5px);
        background: white !important;
        border-color: var(--brand-color) !important;
    }
    .transition-all {
        transition: all 0.3s ease;
    }
</style>
{% endblock %}
